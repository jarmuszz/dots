command "* firefox" firefox
command "* discord" discord
command "* urxvt"   urxvt
command "* kill"    xkill

# Snap to groups by default
sticky yes

# Discord should spawn at group #3
autogroup 3 "Discord,Lightcord"


# Bar
ignore bar      # lemonbar
gap 19 2 2 2    # 17 - lemonbar width
autogroup 0 bar # omnipresent

# Move amount
moveamount 5

# How far windows should snap to edges
snapdist 20

# Width of the border
borderwidth 2

# Keybindings
bind-key XF86AudioRaiseVolume  "amixer -q set Master 2%+ unmute"
bind-key XF86AudioLowerVolume  "amixer -q set Master 2%- unmute"
bind-key XF86AudioMute         "amixer -q set Master toggle"
bind-key XF86AudioMicMute      "amixer -q set Capture toggle"
bind-key XF86MonBrightnessUp   "xbacklight +2"
bind-key XF86MonBrightnessDown "xbacklight -2"

# Theme changing
bind-key 4-p									 "cht"

# Screenshots
bind-key 4-Print               "sh -c 'maim ~/ltmp/$(date +%Y-%m-%d:%H:%M:%S).png'"
bind-key 4S-Print              "sh -c 'maim -s ~/ltmp/$(date +%Y-%m-%d:%H:%M:%S).png'"

# Note taking
bind-key 4-n                   "sh -c 'urxvt -e nvim ~/.cache/notes.md'"

# Plumber
bind-key 4-q                   "xplum"
bind-key 4S-q                  "xplum -s"
bind-key 4-e                   "xplum -c"
bind-key 4S-e                  "xplum -c -s"

# Colors
color activeborder   "{color1}"
color font           "{foreground}"
color selfont        "{foreground}"
color groupborder    "{color2}"
color inactiveborder "{foreground}"
color menubg         "{background}"
color menufg         "{color7}"
color urgencyborder  "{foreground}"
color ungroupborder  "{color1}"
